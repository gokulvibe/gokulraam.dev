---
interface Props {
  value: string;
  label: string;
}
const { value, label } = Astro.props;
---

<div class="flex flex-col">
  <span class="font-display text-3xl text-ember">{value}</span>
  <span class="label mt-1">{label}</span>
</div>
